import { Minus, Plus } from 'lucide-react';
export function QuantitySelector({value,onChange,min=1}:{value:number;onChange:(value:number)=>void;min?:number}){return <div className="inline-flex items-center rounded-xl border border-slate-300 bg-white" aria-label="Quantity selector"><button type="button" className="grid min-h-12 min-w-12 place-items-center" onClick={()=>onChange(Math.max(min,value-1))} aria-label="Decrease quantity"><Minus size={18}/></button><span className="min-w-10 text-center font-bold" aria-live="polite">{value}</span><button type="button" className="grid min-h-12 min-w-12 place-items-center" onClick={()=>onChange(value+1)} aria-label="Increase quantity"><Plus size={18}/></button></div>}
