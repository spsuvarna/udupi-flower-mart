import { beforeEach,describe,expect,it } from 'vitest'; import { CART_STORAGE_KEY,clearStoredCart,loadCart,saveCart } from '../utils/storage';
describe('localStorage cart',()=>{beforeEach(()=>localStorage.clear());it('saves and loads cart items',()=>{const items=[{productId:'p1',quantity:2}];saveCart(items);expect(loadCart()).toEqual(items)});it('returns an empty cart for invalid data',()=>{localStorage.setItem(CART_STORAGE_KEY,'not json');expect(loadCart()).toEqual([])});it('clears stored cart',()=>{saveCart([{productId:'p2',quantity:1}]);clearStoredCart();expect(localStorage.getItem(CART_STORAGE_KEY)).toBeNull()})});
